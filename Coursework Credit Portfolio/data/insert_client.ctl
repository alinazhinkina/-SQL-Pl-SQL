OPTIONS (SKIP=1)
LOAD DATA
CHARACTERSET UTF8
INFILE 'client.csv'
BADFILE 'client.bad'
DISCARDFILE 'client.dsc'
TRUNCATE
INTO TABLE creditor.client
FIELDS TERMINATED BY ';'
OPTIONALLY ENCLOSED BY '"'
TRAILING NULLCOLS
( 
  ID,
  CL_NAME,
  DATE_BIRTH DATE "dd.mm.yyyy"
)